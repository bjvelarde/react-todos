import styled from 'styled-components';

const Filter = styled.select`
  padding: 8px 10px;
  margin-top: 10px;
  margin-left: 10px;
  border: 1px solid #dfdfdf;
  border-radius: 14px;

  &:focus {
    outline: none;
  }
`;

export default {
  Filter
};